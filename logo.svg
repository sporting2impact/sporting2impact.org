<svg xmlns="http://www.w3.org/2000/svg"
     width="256" height="256"
     viewBox="0 0 256 256">
  <!-- Background -->
  <rect width="256" height="256" fill="#FFFFFF"/>

  <!-- SPORTING (green) -->
  <text x="50%" y="45%"
        text-anchor="middle"
        font-family="Arial, Helvetica, sans-serif"
        font-size="26"
        fill="#4CAF50">
    SPORTING
  </text>

  <!-- IMPACT² (blue) -->
  <text x="50%" y="62%"
        text-anchor="middle"
        font-family="Arial, Helvetica, sans-serif"
        font-size="42"
        fill="#003366">
    IMPACT<tspan baseline-shift="super" font-size="48">2</tspan>
  </text>
</svg>
